/**
 * EscrowVault — Page wrapper that renders the new Vault Overview.
 * Replaces the old 4-section layout with the new bento-grid design.
 */
import VaultPage from '../components/vault/VaultPage';

const EscrowVault = () => {
    return <VaultPage />;
};

export default EscrowVault;
